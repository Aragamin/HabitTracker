<manifest package="com.example.habits" xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:label="HabitTracker"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HabitTracker">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Будильник для напоминаний -->
        <receiver
            android:name=".reminders.AlarmReceiver"
            android:exported="false" />

        <!-- Rescheduling после перезагрузки и (опционально) после обновления пакета -->
        <receiver
            android:name=".reminders.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <!-- опционально: раскомментировать для Rescheduling после обновления -->
                <!-- <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/> -->
            </intent-filter>
        </receiver>

    </application>
</manifest>
